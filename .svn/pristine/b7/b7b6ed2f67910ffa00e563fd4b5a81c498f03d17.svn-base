/**
 * 初始化queryVehicleCase详情对话框
 */
var BOefullinfoInfoDlg = {
    bOefullinfoInfoData : {}
};

/**
 * 关闭此对话框
 */
BOefullinfoInfoDlg.close = function() {
	var winOpen=Feng.GetFrame("/bOefullinfo/historyDetail");
	if (winOpen!=undefined){
		top.layer.close(winOpen.BOefullinfoInfoDlg.layerIndex);
	}
}

$(function() {

	Yang.Tools.banner('/bOefullinfo/getVehicleImages',$("#checkno").val())
});
